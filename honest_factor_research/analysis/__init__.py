"""Research scripts — empirical methodologies for factor-model evaluation.

Each module is callable as a CLI (``python -m honest_factor_research.analysis.X``)
and produces a markdown report + CSV in the output directory.
"""
